use std::str::FromStr;

grammar;

//IriRef: String = {
//    <s:r"<[^#x00-#x20<>"{}|^`\]*>"> => String::from(&s[1..]),
//};

pub LangTag: String = {
    <s:r"@[a-zA-Z]+(-[a-zA-Z0-9]+)*"> => String::from(&s[1..]),
};

Hex: String = {
    <s:r"[0-9a-fA-F]"> => String::from(s),
};